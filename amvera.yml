meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirements: requirements.txt

run:
  command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
  containerPort: 8080

resources:
  memoryMb: 512
  cpuCores: 0.5
