[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt && pip install werkzeug==2.3.7"]

[start]
cmd = "python -c 'from app import init_db; init_db()' && gunicorn app:app --bind 0.0.0.0:$PORT"
